# Copy this to server/.env for LOCAL development only.
# On Railway you set these in the Variables tab (DATABASE_URL is auto-added by the Postgres plugin).

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskflow
JWT_SECRET=change-this-to-a-long-random-string
PORT=3000
